const { describe } = require('yargs')
const BritishRoyalty = require('./objects')

// test name and parents keys from family tree 
// test method add parent method -> childOf

describe('British Royalties name'), () => {
    test('Name should be a string'), () => {
        let newRoyalty = new BritishRoyalty("Prince Eric")

        expect()
    }
}